<ui:UiWindow x:Class="WebBrowserMinimalist.Views.Windows.pruebas"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WebBrowserMinimalist.Views.Windows"
         xmlns:ui = "http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:controls="clr-namespace:WebBrowserMinimalist.Views.Controls"
        mc:Ignorable="d"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Acrylic"
             ResizeMode="NoResize"
             WindowStyle="None"
            WindowStartupLocation="CenterScreen"
             WindowCornerPreference="Round"
             Topmost="True"
        Title="pruebas" SizeToContent="WidthAndHeight" Width="auto" Height="auto" MinHeight="0" MinWidth="0" Background="{x:Null}">
    <Grid >
        <!--<ListView x:Name="descargas">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding url}"></TextBlock>
                        <TextBlock Text="{Binding bytes}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
        <controls:Home></controls:Home>
    </Grid>
</ui:UiWindow>
