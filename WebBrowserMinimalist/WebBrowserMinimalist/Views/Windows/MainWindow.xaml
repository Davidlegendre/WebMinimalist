<Window
    x:Class="WebBrowserMinimalist.Views.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:VM="clr-namespace:WebBrowserMinimalist.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:WebBrowserMinimalist.Views.Controls"
    Title="aisdhi"
    d:DesignWidth="1100"
    MinWidth="484"
    d:DesignHeight="650"  
    mc:Ignorable="d"
    WindowStartupLocation="CenterScreen" StateChanged="Window_StateChanged"
     WindowState="Maximized">
    <Window.DataContext>
        <VM:MainWindowViewModel></VM:MainWindowViewModel>
    </Window.DataContext>
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1" CaptionHeight="38"></WindowChrome>
    </WindowChrome.WindowChrome>
    <!--<Grid Margin="{Binding MarginWindowState}" Background="{ui:ThemeResource}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <controls:SearchBar></controls:SearchBar>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70*"></ColumnDefinition>
                <ColumnDefinition Width="270"/>
            </Grid.ColumnDefinitions>
            <Grid Name="ContentBrowser" ClipToBounds="True"></Grid>
            <ListView Name="list" Grid.Column="1" ItemsSource="{Binding TabItems, Mode=OneWay}" SelectedIndex="0" SelectionChanged="ListView_SelectionChanged">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel></StackPanel>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid>
                                <Image Name="img" Source="{Binding ImagenUrl}" Stretch="Uniform" Height="14" Width="14" Margin="7,0"></Image>
                                <ui:ProgressRing Name="progress" Margin="5,0" IsVisibleChanged="ProgressRing_IsVisibleChanged" HorizontalAlignment="Center" IsIndeterminate="True" Visibility="{Binding ProgressVisible}" VerticalAlignment="Center" Height="17" Width="17"></ui:ProgressRing>
                            </Grid>
                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"></RowDefinition>
                                    <RowDefinition Height="*"></RowDefinition>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding TitleDocument}"></TextBlock>
                                <Label Grid.Row="1" Content="{Binding URLNavigated}"></Label>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
                
            </ListView>
        </Grid>
        --><!--  The title bar contains window navigation elements and some Tray related extras.  -->
        <!--  You can put additional controls in the header, such as a search bar.  -->
        <!--  <ui:TitleBar.Header />  -->
        <!--<ui:TitleBar
            Title="{Binding ApplicationTitle, Mode=OneWay}"
            Grid.Row="0"
            Height="34"
            Padding="5"
            WindowChrome.IsHitTestVisibleInChrome="False"
            Icon="pack://application:,,,/Assets/applicationIcon-256.png"
            ShowMaximize="False"
            ShowClose="False"
            ShowMinimize="False">
        </ui:TitleBar>--><!--
    </Grid>-->
    <Grid Margin="{Binding MarginWindowState, Mode=OneWay}">
        <Rectangle Fill="{ui:ThemeResource}" Opacity="0.4"></Rectangle>
        <Grid Name="content"></Grid>
        <Grid Margin="0,48,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="7">
                <ui:Button Name="addbutton" Click="addbutton_Click" Grid.Column="0" Margin="0,0,4,0" WindowChrome.IsHitTestVisibleInChrome="True" Appearance="Transparent" BorderThickness="0" BorderBrush="Transparent" VerticalAlignment="Stretch" >
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Add16"></ui:SymbolIcon>
                        <Label Content="Agregar Pestaña" Margin="7,0,0,0"></Label>
                    </StackPanel>
                </ui:Button>
            </StackPanel>
            <ListView Name="lista" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled"  Grid.Column="1" ItemsSource="{Binding Items, Mode=OneWay}" SelectedIndex="0" SelectionChanged="ListBox_SelectionChanged">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Margin="7"></WrapPanel>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Name="card" VerticalContentAlignment="Stretch" Grid.Row="1" VerticalAlignment="Stretch" Height="270" Width="170">
                            <Grid >
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Orientation="Vertical" >
                                        <Image Source="{Binding IMg}" Margin="2,2,2,10" HorizontalAlignment="Left" Height="22" Stretch="Uniform"></Image>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"></ColumnDefinition>
                                                <ColumnDefinition Width="auto"></ColumnDefinition>
                                            </Grid.ColumnDefinitions>
                                            <ui:SymbolIcon Grid.Column="1" HorizontalAlignment="Left" Visibility="{Binding SoundVisibility,Mode=OneWay}" Symbol="Speaker224" Margin="2,0,6,7" FontSize="16"></ui:SymbolIcon>
                                            <ui:SymbolIcon Symbol="Shield24" HorizontalAlignment="Left" Margin="2,0,6,7" FontSize="16" Filled="True"></ui:SymbolIcon>

                                        </Grid>
                                    </StackPanel>
                                    <Grid Grid.Row="1">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="auto"></RowDefinition>
                                            <RowDefinition Height="auto"></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="{Binding TitleDoc}" Margin="2" MaxHeight="60" TextTrimming="CharacterEllipsis" TextWrapping="Wrap" FontFamily="Segoe UI Semibold" FontSize="14" ></TextBlock>

                                        <TextBlock Grid.Row="1" Text="{Binding Source}" MaxHeight="60"  Margin="2" TextTrimming="CharacterEllipsis" TextWrapping="Wrap" Opacity="0.5" FontFamily="Segoe UI Semibold" FontSize="12" ></TextBlock>

                                    </Grid>
                                </Grid>
                                <ui:Button Name="btnClose" Grid.Row="1" Margin="2" HorizontalAlignment="Stretch" Appearance="Secondary" Click="btnClose_Click" >
                                    <StackPanel Orientation="Horizontal">
                                        <ui:FontIcon Glyph="&#xE106;" FontSize="14"></ui:FontIcon>
                                        <Label Content="Eliminar" FontFamily="Segoe UI Semibold" Margin="7,0,0,0" FontSize="14"></Label>
                                    </StackPanel>
                                </ui:Button>
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
      
</Window>
